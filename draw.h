#ifndef DRAW_H
#define DRAW_H

#include <QWidget>
#include "stable.h"
#include "config.h"

class draw : public QWidget
{
    Q_OBJECT
public:
    explicit draw(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // DRAW_H
